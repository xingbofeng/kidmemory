/**
 * Cloud API contract entrypoint.
 *
 * This subpath re-exports the generated OpenAPI types for the cloud API
 * surface so consumers can import from `@kidmemory/protocol/cloud-api`
 * instead of reaching into generated paths directly.
 */
export type * from '../../generated/cloud-api/ts/index.js';
