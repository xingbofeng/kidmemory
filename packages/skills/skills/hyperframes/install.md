# Hyperframes Install Guide (KidMemory)

1. Ensure Hyperframes CLI is available:

```bash
npm install -g hyperframes
```

2. Initialize or update `hyperframes.json` with registry paths.
3. Install required blocks/components:

```bash
hyperframes add data-chart
hyperframes add grain-overlay
```

4. Wire installed registry items into composition HTML.
